# CaptionIt

CaptionIt is a local-first image captioning app for building dataset captions quickly.
It scans a folder of images, captions them using OpenAI-compatible model backends, and saves editable
`.txt` files next to each image.
